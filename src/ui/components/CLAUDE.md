# components/

One directory per component family (`Button/Button.tsx` + `index.ts`), siblings prefixed like `CardHeader.tsx`, everything re-exported through the barrel. Styling lives in `assets/styles/`; screen-specific pieces belong in `views/`.
